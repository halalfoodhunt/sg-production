<%= form_for(@caterer_service_type) do |f| %>
  <% if @caterer_service_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@caterer_service_type.errors.count, "error") %> prohibited this caterer_service_type from being saved:</h2>

      <ul>
      <% @caterer_service_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
