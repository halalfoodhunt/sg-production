<%= form_for(@raw_food_product_category) do |f| %>
  <% if @raw_food_product_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@raw_food_product_category.errors.count, "error") %> prohibited this raw_food_product_category from being saved:</h2>

      <ul>
      <% @raw_food_product_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
