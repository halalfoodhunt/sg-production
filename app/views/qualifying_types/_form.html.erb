<%= form_for(@qualifying_type) do |f| %>
  <% if @qualifying_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@qualifying_type.errors.count, "error") %> prohibited this qualifying_type from being saved:</h2>

      <ul>
      <% @qualifying_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :halal_summary %><br>
    <%= f.text_area :halal_summary %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
